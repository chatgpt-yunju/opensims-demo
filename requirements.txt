# OpenSims 核心依赖
requests>=2.28.0
pyinstaller>=5.0
python-dotenv>=0.19.0  # 支持 .env 文件加载

# Web API
fastapi>=0.95.0
uvicorn>=0.21.0
pydantic>=1.10.0

# 小红书 - 官方API（可选）
# 无需额外依赖，使用requests

# 小红书 - Playwright自动化（可选）
playwright>=1.40.0

# MCP插件（可选）
mcp>=0.9.0

# 开发依赖（测试）
pytest>=7.0.0
pytest-asyncio>=0.21.0
